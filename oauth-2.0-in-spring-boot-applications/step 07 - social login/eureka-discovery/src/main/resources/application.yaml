server:
  port: 8761
spring:
  application:
    name: eureka-discovery

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    root: DEBUG
    sun.rmi: ERROR
    com:
      netflix:
        eureka: OFF
        discovery: OFF

